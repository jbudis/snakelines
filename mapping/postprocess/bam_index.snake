rule bam_index:
    input:
        '{path}/{sample}.bam'
    output:
        '{path}/{sample}.bam.bai'
    shell:
        '''
        samtools sort {input} -o {input};
        samtools index {input}
        '''
