
rule unzip_fastq:
    input:
        '{file}.fastq.gz'
    output:
        temp('{file}.fastq')
    threads:
        int(config['threads'])
    shell:
        '''
        pigz \
            --decompress \
            --keep \
            --processes {threads} \
            {input}
        '''
